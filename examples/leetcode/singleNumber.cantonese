/*
 * 给定一个非空整数数组，除了某个元素只出现一次以外，其余每个元素均出现两次。
 * 找出那个只出现了一次的元素。
 */
介紹返 |解| 係 乜X {
    佢個老豆叫 |object|
    佢有啲咩? |数组| => {
        介紹返 |@@数组| 係 |数组|
    }
    佢識得 |揾单个数| => {
        介紹返 |结果| 係 0
        i 从 0 行到 |自己嘅 数组 嘅长度|
            介紹返 |结果| 係 |结果 异或 自己嘅 数组[i]|
        到步
        畀我睇下 |结果| 點樣先?
    }
}

[2, 2, 1] 拍住上 => |测试1|
好心 解(测试1) -> 揾单个数 啦
[4, 1, 2, 1, 2] 拍住上 => |测试2|
好心 解(测试2) -> 揾单个数 啦