/*
 * Cantonese 实现 KNN 算法
*/
{% std::macros::math %}

使下 py::math

/*
 * 使用KNN前必须定义3个变量且变量名相同: 数据, 标签, K
*/
|[[5, 1], [4, 0], [1, 3], [0, 4]]| 拍住上 => |数据|
|['动作片', '动作片', '科幻片', '科幻片']| 拍住上 => |标签|
介紹返 |K| 係 3
嗌 KNN 过嚟估下 => |[3, 0]|