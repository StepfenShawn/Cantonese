/*
    Cantonese实现插入排序
*/
介紹返 $insert_sort 
    |数组| 點部署
        |索引| 从 0 行到 |数组 嘅长度|
            介紹返 |当前索引| 係 |索引 减 1|
            介紹返 |当前元素| 係 |数组[索引]|
            落操场玩跑步
                介紹返 |数组[当前索引 加 1]| 係 |数组[当前索引]|
                介紹返 |当前索引| 係 |当前索引 减 1|
            玩到 |当前索引 < 0 或者 数组[当前索引] 比唔上 当前元素| 为止
            介紹返 |数组[当前索引 加 1]| 係 |当前元素|
        到步
        畀我睇下 |数组| 點樣先?
    搞掂

insert_sort 下 -> [64, 34, 25, 12, 22, 11, 90] 啦
insert_sort 下 -> [21, 22, 90, 12, 55, 77, 97] 啦