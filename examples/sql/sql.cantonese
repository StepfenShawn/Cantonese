介紹返 SQL 係 袋仔的法寶 =>
    | (喺 $table: expr 度揾 $columns: expr $(邊個 ($condition: expr))?) => (
        "select " + @扮字($columns) + " from " + @扮字($table) + $(" where " + @扮字($condition))?
    )
    | (睇下 $table: expr) => (
        "select * from " + @扮字($table)
    )
搞掂