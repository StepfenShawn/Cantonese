# TODO: 粵語編程語言內部宏定義

使下 std::*

介紹返 秘訣 係 袋仔的法寶 =>
    | (@x: id 同 @y: id 有幾襯) => { 
        corr 下 -> |@x, @y| 
    }
    | () => { }
搞掂

介紹返 老作一下 係 袋仔的法寶 =>
    | ($()+) => {}
搞掂


介紹返 过嚟估下 係 袋仔的法寶 =>
    | (@model: id => @args: expr) => {
        @model(@args)
    }
搞掂